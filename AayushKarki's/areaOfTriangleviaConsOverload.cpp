// Aayena mam lai sodne wala
//  find the area of the triangle (when its sides are given ) using the concept of oveloaded constructor
#include <iostream>
using namespace std;
class triangle
{
public:
    float s1;
    int s2, s3;
    triangle(float f)
    {
        s1 = f;
    }
    triangle()
    {
        s2 = f1;
        cout << "The area is : " << (1 / 2) * t.s1 * s2;
    }
};

int main()
{
    triangle t(2.2f);
    triangle t1(1);
    // t.area();
}